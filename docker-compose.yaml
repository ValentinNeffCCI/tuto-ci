services:
  app:
    restart: unless-stopped
    container_name: react-starter
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
      - "3001:443"
    volumes:
      - .:/app
      - /app/node_modules
